export type SalesCashierTableRow = {
  periode: string;
  nama_kasir: string;
  transaksi: string;
  omzet: string;
  shift: string;
  rata_rata_transaksi_per_shift: string;
  transaksi_dibatalkan: string;
  transaksi_retur: string;
};
